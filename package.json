{
  "name": "vonsim",
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "format": "prettier -w .",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "cd app && pnpm run dev",
    "docs:dev": "cd docs && pnpm run dev"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "turbo": "^1.10.11"
  }
}
